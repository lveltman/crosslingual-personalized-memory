"""Прогон + метрики (вынесено из train.py).
Метрики: качество персонализации (downstream + LLM-as-judge); critical-fact/negation fidelity
(polarity-acc, Critical Flip Rate); cross-lingual gap (en vs uz). Дамп по-примерно → analyze_bootstrap."""
